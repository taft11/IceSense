{
  "rules": {
    ".read": "auth != null",
    "IoT": {
      ".read": "auth != null",
      ".write": "auth != null && (auth.token.admin === true || auth.token.email === 'marelynflorento@gmail.com')"
    },
    "inventory": {
      "scale_1": {
        ".read": "auth != null",
        ".write": "auth != null && (auth.token.admin === true || auth.token.email === 'marelynflorento@gmail.com')",
        "$section": {
          "sacks_breakdown": {
            "$stockKey": {
              ".validate": "newData.isNumber() && newData.val() >= 0"
            }
          }
        }
      }
    }
  }
}